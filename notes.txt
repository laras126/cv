Larva for anyone
define tokens in the project, not in larva tokens so they don't have to be committed - option to override tokens location?
ability to have files at root of patterns or src/
possible to remove the step of having prototype file
need a way to add local css and still use css from the packages
assign color-XX to other color tokens by default